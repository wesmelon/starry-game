{
  "name": "starry-little-days",
  "version": "1.0.0",
  "description": "A cozy toddler life sim, procedurally drawn and chiptuned, in TypeScript.",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "bundle": "esbuild src/main.ts --bundle --format=iife --target=es2020 --outfile=dist/game.js",
    "build": "npm run typecheck && npm run bundle",
    "watch": "esbuild src/main.ts --bundle --format=iife --target=es2020 --outfile=dist/game.js --watch",
    "check": "npm run build && node dev/check.js && node dev/smoke.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
